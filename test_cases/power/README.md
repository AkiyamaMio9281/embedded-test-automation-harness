# Power Tests

Idle and load power consumption checks. Captures per-sample `power_mw`
and the run's `peak_power_mw` and asserts both stay under their budgets.

Driven by `POWER-001` in `test_plans/smoke.yaml`.
